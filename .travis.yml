language: generic

services:
  - docker

before_install:
- docker pull testthedocs/ttd-linkcheck
- docker pull testthedocs/ttd-remark

script:
- docker run -v `pwd`:/srv/test testthedocs/ttd-linkcheck docs
- docker run --rm -i -v $PWD:/lint/input:ro testthedocs/ttd-remark docs